<%= @ticker.name %>

<% stuff = {} %>
<% @ticker.prices.order(:created_at).each do |price_obj| %>
  <% datet = Time.at(price_obj.timestamp.to_i) %>
  <% val = price_obj.value %>
  <% stuff[datet] = val %>
<% end %>

<%= area_chart stuff, curve: false, points: false %>
